apiVersion: v1
kind: Service
metadata:
  name: icondo-frontend
  namespace: icondo
  labels:
    app: icondo
    tier: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: icondo
    tier: frontend
